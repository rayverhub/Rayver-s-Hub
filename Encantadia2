local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Rayver Hub | Encantadia Chronicles Sanggre 2",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Encantadia Chronicles Sanggre 2",
   LoadingSubtitle = "by Rayver",
   ShowText = "Encantadia", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "E", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = true, -- Set this to true to use our key system
   KeySettings = {
      Title = "Encantadia Chronicles Sanggre 2",
      Subtitle = "Key System",
      Note = "Key: rayverhub", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = false, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"rayverhub"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local Tab = Window:CreateTab("Character", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Kambal Diwa ng Brilyante")
local Button = Tab:CreateButton({
   Name = "Alipato (PATCHED)",
   Callback = function()
        local args = {
""
}
game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("ChooseCharacter"):FireServer(unpack(args))
   end,
})

local Button = Tab:CreateButton({
   Name = "Avilan (PATCHED)",
   Callback = function()
        local args = {
""
}
game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("ChooseCharacter"):FireServer(unpack(args))
   end,
})

local Button = Tab:CreateButton({
   Name = "Agua (PATCHED)",
   Callback = function()
        local args = {
""
}
game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("ChooseCharacter"):FireServer(unpack(args))
   end,
})

local Button = Tab:CreateButton({
   Name = "Sari-a (PATCHED)",
   Callback = function()
        local args = {
""
}
game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("ChooseCharacter"):FireServer(unpack(args))
   end,
})

local Section = Tab:CreateSection("Mineave")
local Button = Tab:CreateButton({
   Name = "Anaca (PATCHED)",
   Callback = function()
        local args = {
""
}
game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("ChooseCharacter"):FireServer(unpack(args))
   end,
})

local Tab = Window:CreateTab("Abilities", 4483362458) -- Title, Image
local Section = Tab:CreateSection("Lupa Abilities")
local Button = Tab:CreateButton({
   Name = "Earthquake",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("EarthquakeActivate"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Heal",
   Callback = function()
        local args = {
"Self"
}
game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("LupaHealRequest"):FireServer(unpack(args))
   end,
})

local Section = Tab:CreateSection("Apoy Abilities")
local Button = Tab:CreateButton({
   Name = "Mass Burn",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("MassBurnEvent"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Shapeshift (For Pirena Only!)",
   Callback = function()
        game.Players.LocalPlayer.PlayerGui.Pirena.Shapeshift.Visible = true
   end,
})

local Section = Tab:CreateSection("Shockwave")
local Button = Tab:CreateButton({
   Name = "Shockwave (Mitena)",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("MitenaShockwave"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Shockwave (Agua)",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("AguaShockwaveEvent"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Shockwave (Avilan)",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("AvilanShockwaveRemote"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Shockwave (Lira)",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("LiraShockwaveEvent"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Shockwave (Mira)",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("MiraShockwaveEvent"):FireServer()
   end,
})

local Button = Tab:CreateButton({
   Name = "Shockwave (Terra)",
   Callback = function()
        game:GetService("ReplicatedStorage"):WaitForChild("RemoteEvents"):WaitForChild("TerraShockwaveEvent"):FireServer()
   end,
})

local Tab = Window:CreateTab("Brilyante Location", 4483362458) -- Title, Image
local Button = Tab:CreateButton({
   Name = "Brilyante ng Apoy (Hathoria)",
   Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-1200.17163, 78.2969131, -1014.03601, 0.217469692, -8.15687571e-08, 0.976067066, 8.46638102e-08, 1, 6.47055387e-08, -0.976067066, 6.85660595e-08, 0.217469692)
   end,
})

local Button = Tab:CreateButton({
   Name = "Brilyante ng Hangin (Lireo)",
   Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1004.12494, 26.3312416, -0.0191973094, 0.0242077019, -2.42121505e-08, -0.999706924, 3.25364695e-08, 1, -2.34313831e-08, 0.999706924, -3.19597149e-08, 0.0242077019)
   end,
})

local Button = Tab:CreateButton({
   Name = "Brilyante ng Tubig (Adamya)",
   Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-2453.49414, -24.565815, 376.617859, 0.939264774, 7.21551245e-08, 0.343193352, -8.31670377e-08, 1, 1.73684871e-08, -0.343193352, -4.48559838e-08, 0.939264774)
   end,
})

local Button = Tab:CreateButton({
   Name = "Brilyante ng Lupa (Sapiro)",
   Callback = function()
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(612.588013, 107.488457, -1164.5863, 0.999299109, 1.13432446e-08, -0.0374332145, -1.27077318e-08, 1, -3.62133292e-08, 0.0374332145, 3.66636428e-08, 0.999299109)
   end,
})
